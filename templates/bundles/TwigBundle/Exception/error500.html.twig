{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mt-5">
        <div class="jumbotron text-center">
            <h1 class="block text-danger">Oops! la page est introuvable</h1>
            <h2 class="block text-danger">"{{ status_code }} {{ status_text }}".</h2>
            <p class="block mt-5"><a href="{{ path("agence") }}" class="btn btn-primary mr-5">Acceuil</a><a href="{{ path("agence") }}" class="btn btn-success">Contact</a></p>
        </div>
    </div>
{% endblock %}
